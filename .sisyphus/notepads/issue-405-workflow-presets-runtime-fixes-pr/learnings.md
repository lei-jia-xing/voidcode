2026-05-02 - Task 1 prompt materialization: partial request agent overrides merge in `VoidCodeRuntime._config_with_request_agent_override()`. `prompt_materialization` must follow the same "request value wins, otherwise preserve resolved agent" rule as prompt/prompt_ref/prompt_source so custom markdown prompts remain visible to `render_agent_prompt()` after model-only overrides.
2026-05-02 - Prompt materialization regression tests using provider-engine model overrides should inject a tiny local `ModelProviderRegistry` instead of relying on the default registry. This keeps model-only override assertions robust while preserving the exact merge behavior under test.

2026-05-02 - Task 2 manifest_dir safety: local custom tool command validation now checks every command part that contains `${manifest_dir}` after rendering, both at manifest discovery and immediately before `_run_command()`. Tests use a shallow `tools/` fixture path for `${manifest_dir}/../../outside-workspace/script.py` so the traversal genuinely escapes the workspace; `.voidcode/tools` would only resolve back to the workspace root.

2026-05-02 - Task 2 follow-up: `${manifest_dir}` validation must inspect the path segment introduced by substitution, not the whole command argument. Embedded forms such as `--script=${manifest_dir}/echo.py` are safe when the extracted rendered path stays under the workspace, while `--script=${manifest_dir}/../../outside-workspace/script.py` must fail during manifest/provider validation before `_run_command()` can execute.

2026-05-02 - Task 3 local custom argument transport: `LocalCustomTool._build_environment()` now keeps only safe runtime context env vars; full `call.arguments` continue to flow exclusively through stdin via `_invoke(... input_text=json.dumps(call.arguments))`. Regression coverage uses `super-secret-token` to prove stdin receives structured args while no child env value contains the secret or serialized payload.

2026-05-02 - Task 3 doc verification follow-up: after removing `VOIDCODE_TOOL_ARGUMENTS`, grep for that name should only find tests asserting absence or historical changelog entries if any. Runtime contract docs must describe local custom tool arguments as JSON stdin-only, with env limited to bounded runtime context.
2026-05-02 - Task 4 workflow presets: runtime-owned declarations now live in `src/voidcode/runtime/workflow.py` with frozen `WorkflowPreset`/`WorkflowMcpBindingIntent` models, builtin `research`/`implementation`/`frontend`/`review`/`git` presets, duplicate-id checks, closed payload parsing, hook preset validation, hard validation for missing force-loaded skills, required MCP binding availability failures, and optional MCP binding representation for later degraded snapshots. `runtime/config.py` accepts a declaration-only `workflows` section and `runtime/config_schema.py` exposes closed workflow preset/MCP intent schemas without wiring runtime preset selection.
2026-05-02 - Task 4 verification fix: repo-local `workflows.*.force_load_skills` validation now derives available names from the existing `SkillRegistry.discover()` path, using configured `skills.paths` or the default `.voidcode/skills`. Required MCP server intents still validate against configured `mcp.servers`; no MCP profile catalog exists in runtime config today, so profile validation remains available only when callers explicitly provide profile names to the workflow validator.
2026-05-02 - Task 4 verification fix 2: `load_runtime_config()` now discovers local skills for workflow validation only when repo-local `workflows` is present. Workspaces with malformed `.voidcode/skills/**/SKILL.md` no longer fail ordinary config loading without workflow declarations, while declared workflows still validate forced skills against discovered names.
2026-05-02 - Task 5 workflow preset metadata: `workflow_preset` is a strict runtime request metadata key and effective config selection resolves repo-local workflows before builtin presets. Preset defaults are applied through the same request-agent merge path before explicit request agent/model overrides, preserving custom materialization when request overrides omit `prompt_materialization` while allowing explicit request materialization to win.
2026-05-02 - Task 5 verification fix: fresh top-level `workflow_preset` selection must not call request-agent merge with `allow_subagent_presets=True`. Child-only workflow defaults such as builtin `review`/`advisor` are accepted as strict metadata but do not replace the active top-level agent until a later task defines safe fresh-run application/snapshot behavior.

2026-05-02 - Task 6 fresh workflow presets: `VoidCodeRuntime._request_metadata_with_workflow_defaults()` now resolves selected workflow presets before session metadata/capability snapshots/skill loading. The persisted `runtime_config.workflow`, top-level `workflow`, and `agent_capability_snapshot.workflow` payloads record only audit-safe preset ids/category/effective agent applicability/read-only and policy refs/guidance/MCP intents; child-only defaults such as `research` remain metadata-only and do not replace the top-level leader agent.

2026-05-02 - Task 7 workflow snapshots: fresh runs now pass the already-materialized `workflow` snapshot into `runtime_config.workflow` instead of re-resolving the preset, keeping top-level metadata, runtime config, capability snapshot, resume/debug/replay, and session bundle export aligned even if repo-local or builtin preset definitions drift later. Workflow snapshots include `snapshot_version`, source, policy refs, verification guidance, read-only default, skill refs/force-load refs, hook refs, and MCP intent availability/degraded metadata without raw args/stdin/env/secrets.

2026-05-02 - Task 8 delegated workflow inheritance: delegated child metadata now carries an audit-safe workflow snapshot through strict request metadata. Child runs reuse an inherited parent snapshot when available, explicit child workflow overrides must resolve before task execution and default to the selected delegated child preset, and active parent metadata is refreshed after workflow default materialization so task-tool delegation can inherit the effective parent snapshot before persistence.
2026-05-02 - Task 9 builtin presets: MVP workflow registry remains exactly research/implementation/frontend/review/git. Git now uses declarative git_safety tool policy plus runtime_default permissions and explicit prompt/verification guidance forbidding destructive operations, hook bypass, force push/hard reset/history rewrite unless explicitly requested and approved. Focused builtin workflow/preset tests and dedicated git safety filter pass with evidence under .sisyphus/evidence/task-9-builtin-presets.txt and task-9-git-safety.txt.

2026-05-02 - Task 10 docs: added `docs/contracts/workflow-presets.md` and indexed it from `docs/contracts/README.md`. The contract now states the five builtin presets, request metadata precedence, prompt materialization append rules, persisted snapshot truth, delegated child inheritance, and conservative runtime-governed safety composition while keeping Husky, marketplace, workflow DAG, and executable DSL out of scope.

2026-05-02 - Task 11 final verification: targeted regression commands and `mise run check` all passed; the remaining LSP diagnostics are unrelated pre-existing ty hints in other test files outside this task scope. Next step was clean commit/push/PR creation with the scope kept free of Husky/native hook workflow changes.
